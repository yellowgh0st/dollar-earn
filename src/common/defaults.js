const defaults = {}

defaults.api = {}
defaults.api.esd = {}
defaults.api.esd.pools = 'https://esd.tools/api/pools'

defaults.network = {}
defaults.network.chainId = 1
defaults.network.connectors = undefined

export default defaults
